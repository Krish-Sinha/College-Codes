// Merge K sorted arrays using using heap

#include <bits/stdc++.h>
using namespace std;

#define N 4

int main()
{
    int arr[][N] = {{2, 6, 12, 34},
                    {1, 9, 20, 1000},
                    {23, 34, 90, 2000}};

    int size = (sizeof(arr) / sizeof(arr[0]));
    int output[size * N];
}